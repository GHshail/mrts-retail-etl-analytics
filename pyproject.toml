[project]
name = "mrts-etl"
version = "1.0.0"
description = "Production-style ETL and time-series analysis for the U.S. Census MRTS dataset"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
